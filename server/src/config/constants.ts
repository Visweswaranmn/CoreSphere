/** Static service metadata surfaced by the health endpoint and logs. */
export const SERVICE_NAME = 'coresphere-api';
export const APP_VERSION = process.env.npm_package_version ?? '0.1.0';
export const API_PREFIX = '/api/v1';
